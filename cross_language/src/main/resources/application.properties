# Spring Boot Application Configuration for Apache Arrow Consumer

# Application name
spring.application.name=Apache Arrow Consumer

# Server configuration (optional - only if using web endpoints)
server.port=8080

# Arrow data file location
arrow.data.file=shared_data.feather

# Logging configuration
logging.level.com.example.arrow=INFO
logging.level.org.apache.arrow=WARN

